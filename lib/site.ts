// Canonical production URL — used by sitemap, robots and metadata.
// Hardcoded (not the env var) so the sitemap never points at a preview URL.
export const SITE_URL = 'https://sportslabacademy.com.au'
